package com.example.student.skywalkerairlines.utilities;

import com.example.student.skywalkerairlines.data.Flight;

import org.joda.time.DateTime;
import org.joda.time.Hours;
import org.joda.*;
import java.util.ArrayList;

public class Search {
//    private Sorting sort = new Sorting();
//    public ArrayList<Flight> searchBySource(ArrayList<Flight> flights, String source, String dest){
//        ArrayList<Flight> fl = new ArrayList<Flight>();
//        for(Flight f : flights) {
//            if(f.getSourceID().equalsIgnoreCase(source)&&f.getSourceID().equalsIgnoreCase(dest)) {
//                fl.add(f);
//            }
//        }
//        fl = sort.sortedFlights(fl, source, dest);
//        return fl;
//    }
//
//    public ArrayList<Employee> searchEmp(ArrayList<Employee> emp, int flightTime){
//        ArrayList<Employee> compEmp= new ArrayList<Employee>();
//        for(Employee e : emp) {
//            if(Hours.hoursBetween(e.getClockoutTime(), e.getClockinTime()).isGreaterThan(Hours.EIGHT) &&
//                    Hours.hoursBetween(e.getClockoutTime(), e.getClockinTime()).getHours()+flightTime>8) {
//                compEmp.add(e);
//            }
//        }
//        compEmp = sort.sortedEmployee(compEmp);
//        return compEmp;
//    }
}
